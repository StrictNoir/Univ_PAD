services:
  webui:
    build:
      context: WebUI
      dockerfile: Dockerfile
    container_name: webui
    restart: unless-stopped
    ports:
      - "9100:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      _URL: http://178.62.201.122:8080  

